module.exports = {
  name: 'cache',
  description: 'Developer Only',
  category: 'Developer',
  devOnly: true,
  async execute(message, args, client) {
    const { modWarnings, modMutes, modBans, commands, prefixCmds, buttons, menus, modals } = client;
  },
};